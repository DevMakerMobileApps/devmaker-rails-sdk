<!-- Layout navbar -->
<nav class="layout-navbar navbar navbar-expand-lg align-items-lg-center container-p-x bg-primary navbar-dark d-print-none" id="layout-navbar">

  <!-- Brand demo (see app/assets/stylesheets/demo.css) -->
  <a href="/" class="navbar-brand app-brand demo d-lg-none py-0 mr-4">
    <span class="app-brand-logo demo">
      <%#= image_tag('logo.png', style: 'height: 50px;') %>
    </span>
    <span class="app-brand-text demo font-weight-normal ml-2">GoBuddies</span>
  </a>

  <% if local_assigns[:hide_toggle] != true %>
  <!-- Sidenav toggle (see app/assets/stylesheets/demo.css) -->
  <div class="layout-sidenav-toggle navbar-nav d-lg-none align-items-lg-center mr-auto">
    <a class="nav-item nav-link px-0 mr-lg-4" href="javascript:void(0)">
      <i class="fas fa-bars text-large align-middle"></i>
    </a>
  </div>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#layout-navbar-collapse">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="navbar-collapse collapse" id="layout-navbar-collapse">
    <!-- Divider -->
    <hr class="d-lg-none w-100 my-2">

    <div class="navbar-nav align-items-lg-center ml-auto">
      <div class="demo-navbar-user nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
          <span class="d-inline-flex flex-lg-row-reverse align-items-center align-middle">
            <span class="px-1 mr-lg-2 ml-2 ml-lg-0"><%= current_admin.email if current_admin.email.present? %></span>
          </span>
        </a>
        <div class="dropdown-menu dropdown-menu-right">
          <%= link_to('#', class: 'dropdown-item') do %>
            <i class="fas fa-cog text-lightest"></i> Minha Conta</a>
          <% end %>
          <div class="dropdown-divider"></div>
          <%= link_to(destroy_user_session_path, method: :delete, class: 'dropdown-item') do %>
            <i class="fas fa-sign-out-alt text-danger"></i> &nbsp; Sair</a>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
<!-- / Layout navbar -->
